import { keyframes } from "@emotion/core";

export const fadeIn = keyframes`
    from {
      opacity: 0;
    }
  
    to {
      opacity: 1;
    }
  
  
  .fadeIn {
    animation-name: fadeIn;
  }`;
